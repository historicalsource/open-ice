*
*PROCESS SLEEP CONSTANT
*P1=SLEEP TIME CONSTANT 1-FFFF LOADED INTO A0
*RET ADDR ON STACK
*
SLEEP	.macro	P1
	.if	P1 < 33
	MOVK	:P1:,A0
	.else	
	MOVI	:P1:,A0
	.endif
	CALLA	PRCSLP
	.endm

